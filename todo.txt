V 1.) Get config file working:
    *Group patches together that need 
    to be patched in a particular order.

V 2.) Get patches into program:
    *Parse config file.

    *Load patch identifiers into memory 
    in the right order as specified in 
    the config file.

3.) Find optimal patch configuration:
    *Disect every patch into its components.

    *Create diff file for every component.

    *sort new patch files for every source file.

4.) Apply patches:
    *Try to apply patches with "git apply" command
